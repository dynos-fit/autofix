"""CLI subpackage for autofix_next.

Exposes the ``autofix-next`` console script entry point (see
``pyproject.toml`` → ``[project.scripts]``) as
``autofix_next.cli.main:main``. The ``scan`` subcommand is implemented in
``autofix_next.cli.scan_command``.
"""
