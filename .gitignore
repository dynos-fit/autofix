__pycache__/
*.pyc
.pytest_cache/
.autofix/
# autofix LLM-judgment per-key prompt cache (per-scan, not for VCS)
.autofix/cache/
.dynos/
latest-llm-prompt.txt
latest-llm-prompts/
benchmarks/agent_bench/out/

# Test-run byproduct: scip-go state dir created inside the fixture tree
tests/autofix_next/languages/fixtures/mixed_repo/go/.autofix-next/

# install.sh / pip editable build artifacts
autofix_scanner.egg-info/
# dynos-work calibration output (auto-regenerated)
benchmarks/generated/

# autofix LLM prompt-prefix cache (per-scan, not for VCS)
.cache/
