"""CLI subpackage for autofix.

Exposes the ``autofix`` console script entry point (see
``pyproject.toml`` → ``[project.scripts]``) as
``autofix.cli.main:main``. The ``scan`` subcommand is implemented in
``autofix.cli.scan_command``.
"""
